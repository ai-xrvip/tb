# Core dependencies
python-telegram-bot[job-queue]>=21.0,<22.0
python-dotenv>=1.0.0

# HTTP (async — httpx replaces sync requests)
httpx>=0.27.0

# HTML parsing
beautifulsoup4>=4.12.0

# Image processing (watermark crop)
Pillow>=10.0.0

# Cloudflare bypass for XChina
curl_cffi>=0.7.0

# Web admin dashboard
flask>=2.3.0
waitress>=2.1.0
